/**
 * Compute a modulo value that is positive.
 */
export const mod = (n: number, m: number) => ((n % m) + m) % m;

/**
 * The value is used for some hexagon computations.
 */
export const SQR_3 = Math.sqrt(3);
